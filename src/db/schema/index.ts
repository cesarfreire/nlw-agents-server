import { audioChunks } from "./audio-chunks.ts";
import { questions } from "./questions.ts";
import { rooms } from "./rooms.ts";

// Barrel file
export const schema = {
  rooms,
  questions,
  audioChunks,
};
